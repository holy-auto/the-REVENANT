import type { MetadataRoute } from 'next';import { cases, menus, site, stores } from '@/data/site';export default function sitemap():MetadataRoute.Sitemap{const paths=['/','/prices','/works','/voices','/first-time','/faq','/about','/contact','/privacy',...stores.map(s=>s.slug),...menus.map(m=>m.slug),...cases.map(c=>`/works/${c.slug}`)];return paths.map(p=>({url:site.baseUrl+p,lastModified:new Date(),changeFrequency:'monthly',priority:p==='/'?1:.7}))}

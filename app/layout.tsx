import type { Metadata } from 'next';import { Noto_Sans_JP,Noto_Serif_JP } from 'next/font/google';import './globals.css';import './station.css';import { FixedCTA,Footer,Header,JsonLd } from '@/components/ui';import { site, stores } from '@/data/site';import { Analytics } from '@vercel/analytics/next';
const sans=Noto_Sans_JP({subsets:['latin'],variable:'--font-noto-sans-jp'});const serif=Noto_Serif_JP({subsets:['latin'],variable:'--font-noto-serif-jp'});
export const metadata:Metadata={metadataBase:new URL(site.baseUrl),title:{default:'THE REVENANT｜日立市・鉾田市のカーコーティング専門店',template:'%s｜THE REVENANT'},description:'茨城県日立市・鉾田市のカーコーティング、車磨き、手洗い洗車の相談窓口。初めてでも写真から相談できます。',openGraph:{title:'THE REVENANT',description:site.concept,images:[site.ogImage],locale:'ja_JP',type:'website'},verification:site.gscVerification?{google:site.gscVerification}:undefined,alternates:{canonical:'/'}};
const orgLd={'@context':'https://schema.org','@type':'Organization','@id':`${site.baseUrl}/#organization`,name:site.name,url:site.baseUrl,logo:`${site.baseUrl}${site.ogImage}`,description:site.concept,sameAs:[site.instagramUrl],areaServed:stores.map(s=>({'@type':'City',name:s.area})),department:stores.map(s=>({'@type':'AutoRepair',name:`${site.name} ${s.name}`,url:site.baseUrl+s.slug}))};
const siteLd={'@context':'https://schema.org','@type':'WebSite','@id':`${site.baseUrl}/#website`,name:site.name,url:site.baseUrl,inLanguage:'ja-JP',publisher:{'@id':`${site.baseUrl}/#organization`}};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="ja" className={`${sans.variable} ${serif.variable}`}><body><JsonLd data={orgLd}/><JsonLd data={siteLd}/><Header/>{children}<Footer/><FixedCTA/><Analytics/></body></html>}

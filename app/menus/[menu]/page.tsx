import { notFound } from 'next/navigation';import type { Metadata } from 'next';import { CTA, JsonLd } from '@/components/ui';import { menus, site } from '@/data/site';
export function generateStaticParams(){return menus.map(m=>({menu:m.slug.split('/').pop()!}))}export function generateMetadata({params}:{params:{menu:string}}):Metadata{const m=menus.find(x=>x.slug.endsWith(params.menu));return m?{title:m.name,description:m.summary,alternates:{canonical:m.slug}}:{}}
export default function MenuPage({params}:{params:{menu:string}}){const m=menus.find(x=>x.slug.endsWith(params.menu));if(!m)notFound();return <main><JsonLd data={{'@context':'https://schema.org','@type':'Service',name:m.name,provider:{'@type':'AutoRepair',name:site.name},areaServed:['日立市','鉾田市','茨城県']}}/><section className="section"><div className="container prose-rev"><p className="eyebrow">Service</p><h1>{m.name}</h1><p>{m.summary}</p><h2>必要な施工だけをご提案します</h2><p>塗装状態、保管環境、洗車頻度、予算を確認し、過剰なメニューではなく愛車に合う守り方を一緒に考えます。</p><h2>こんな方へ</h2><p>新車を長くきれいに保ちたい方、経年車のくすみや洗車傷が気になる方、施工後の洗車方法まで相談したい方に向けたメニューです。</p><p>料金目安：{m.price}</p></div></section><CTA/></main>}

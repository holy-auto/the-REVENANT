import { CTA } from '@/components/ui';import { prices } from '@/data/site';export const metadata={title:'料金の目安',description:'カーコーティング、車磨き、手洗い洗車の料金は車種と状態で変わります。目安と見積もりの考え方を掲載。',alternates:{canonical:'/prices'}};export default function Page(){return <main><section className="section"><div className="container"><h1 className="text-4xl font-black">料金の目安</h1><p className="lead mt-4">正式な料金表は未提供情報のため創作せず、後から差し替えやすいデータで管理しています。</p><div className="mt-8 grid gap-4">{prices.map(p=><div className="card p-6" key={p.item}><h2 className="text-xl font-bold">{p.item}</h2><p className="mt-2 text-bronze">{p.range}</p><p className="lead mt-2">{p.note}</p></div>)}</div></div></section><CTA/></main>}

import Link from 'next/link';import { cases } from '@/data/site';export const metadata={title:'施工事例一覧',description:'THE REVENANTの施工事例を追加・更新しやすい形式で掲載します。',alternates:{canonical:'/works'}};export default function Works(){return <main><section className="section"><div className="container"><h1 className="text-4xl font-black">施工事例一覧</h1><div className="mt-8 grid gap-5">{cases.map(c=><Link className="card p-6" href={`/works/${c.slug}`} key={c.slug}><h2 className="text-xl font-bold">{c.title}</h2><p className="lead mt-2">{c.store}／{c.area}／{c.maker} {c.car}</p></Link>)}</div></div></section></main>}
